<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yoparking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YoParking">
        <activity android:name=".rent_parking" />
        <activity android:name=".booked" />
        <activity android:name=".write_reviews" />
        <activity android:name=".otp_confirmation" />
        <activity android:name=".verification_code" />
        <activity android:name=".login_signup" />
        <activity android:name=".option_Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>